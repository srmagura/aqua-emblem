// Generated by CoffeeScript 1.8.0
(function() {
  _cui.ItemInfoBox = (function() {
    function ItemInfoBox(ui, box) {
      this.ui = ui;
      this.box = box;
    }

    ItemInfoBox.prototype.init = function(item, usable) {
      var options, stat, weaponStats, weaponStatsEl, _i, _len;
      options = {
        usable: usable
      };
      this.box.find('.header').html(item.getElement(options));
      weaponStatsEl = this.box.find('.weapon-stats');
      weaponStats = ['hit', 'might', 'crit', 'weight'];
      for (_i = 0, _len = weaponStats.length; _i < _len; _i++) {
        stat = weaponStats[_i];
        weaponStatsEl.find('.' + stat).text(item[stat]);
      }
      weaponStatsEl.find('.range').text(item.range.toString());
      return this.show();
    };

    ItemInfoBox.prototype.show = function() {
      return this.box.show();
    };

    ItemInfoBox.prototype.hide = function() {
      return this.box.hide();
    };

    return ItemInfoBox;

  })();

}).call(this);
