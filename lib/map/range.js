// Generated by CoffeeScript 1.12.2
(function() {
  var indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  window.Range = (function() {
    function Range(min1, max1) {
      var j, ref, ref1, results;
      this.min = min1;
      this.max = max1;
      if (this.max == null) {
        this.max = this.min;
      }
      if (this.min != null) {
        this.array = (function() {
          results = [];
          for (var j = ref = this.min, ref1 = this.max; ref <= ref1 ? j <= ref1 : j >= ref1; ref <= ref1 ? j++ : j--){ results.push(j); }
          return results;
        }).apply(this);
      } else {
        this.array = [];
      }
    }

    Range.prototype.contains = function(i) {
      return indexOf.call(this.array, i) >= 0;
    };

    Range.prototype.union = function(range) {
      var max, min, result;
      min = range.min;
      if (this.min < min) {
        min = this.min;
      }
      max = range.max;
      if (this.max > max) {
        max = this.max;
      }
      result = new Range(min, max);
      result.array = this.array.concat(range.array);
      return result;
    };

    Range.prototype.toString = function() {
      if (this.min === this.max) {
        return "" + this.min;
      } else {
        return this.min + "-" + this.max;
      }
    };

    return Range;

  })();

}).call(this);
